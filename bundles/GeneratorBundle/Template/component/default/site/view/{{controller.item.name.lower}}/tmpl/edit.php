<?php
/**
 * @package     Joomla.Administrator
 * @subpackage  {{extension.element.lower}}
 *
 * @copyright   Copyright (C) 2012 Asikart. All rights reserved.
 * @license     GNU General Public License version 2 or later; see LICENSE.txt
 * @author      Generated by AKHelper - http://asikart.com
 */

// No direct access
defined('_JEXEC') or die;

JHtml::_('behavior.tooltip');
JHtml::_('behavior.formvalidation');
JHtml::_('behavior.keepalive');
{{extension.name.cap}}Helper::_('include.core');


$app = JFactory::getApplication() ;
if( JVERSION >= 3){
    JHtml::_('formbehavior.chosen', 'select');
    if($app->isSite()){
        //{{extension.name.cap}}Helper::_('include.fixBootstrapToJoomla');
    }
}else{
    {{extension.name.cap}}Helper::_('include.bluestork');
    // {{extension.name.cap}}Helper::_('include.fixBootstrapToJoomla');
}



// Init some API objects
// ================================================================================
$date     = JFactory::getDate( 'now' , JFactory::getConfig()->get('offset') ) ;
$doc     = JFactory::getDocument() ;
$uri     = JFactory::getURI() ;
$user    = JFactory::getUser() ;



// For Site
// ================================================================================
if($app->isSite()) {
    {{extension.name.cap}}Helper::_('include.isis');
}



// Edit setting
// ================================================================================
$tabs     = count( $this->fields ) > 1 ? true : false;


if($app->isAdmin()) {
    $span_left  = 8 ;
    $span_right = 4 ;
    
    $width_left = 60 ;
    $width_right= 40 ;
}else{
    $span_left  = 12 ;
    $span_right = 12 ;
    
    $width_left = 100 ;
    $width_right= 100 ;
}

?>
<script type="text/javascript">
    <?php if( $app->isSite() ): ?>
    WindWalker.fixToolbar(0, 300) ;
    <?php endif; ?>
    
    Joomla.submitbutton = function(task)
    {
        if (task == '{{controller.item.name.lower}}.cancel' || document.formvalidator.isValid(document.id('{{controller.item.name.lower}}-form'))) {
            Joomla.submitform(task, document.getElementById('{{controller.item.name.lower}}-form'));
        }
        else {
            alert('<?php echo $this->escape(JText::_('JGLOBAL_VALIDATION_FORM_FAILED'));?>');
        }
    }
</script>

<div id="{{extension.name.cap}}" class="windwalker {{controller.item.name.lower}} editform <?php echo (JVERSION >= 3) ? 'joomla30' : 'joomla25' ?>">

<form action="<?php echo JRoute::_( JFactory::getURI()->toString() ); ?>" method="post" name="adminForm" id="{{controller.item.name.lower}}-form" class="form-validate" enctype="multipart/form-data">    
    
    <!-- Tab Bodys -->
    <?php echo $tabs ? {{extension.name.cap}}Helper::_('panel.startTabs', '{{controller.item.name.lower}}Tab', array( 'active' => $this->fields[0] ) ) : null ; ?>
        <?php foreach( $this->fields as $key => $group ): 
                $fieldsets = $this->form->getFieldsets($group) ;
                
                echo $tabs ? {{extension.name.cap}}Helper::_('panel.addPanel' , '{{controller.item.name.lower}}Tab', $this->fields_group[$key]['label'] ? $this->fields_group[$key]['label'] : JText::_('{{extension.element.upper}}_EDIT_FIELDS_'.$group) , $group ) : null ;
        ?>
            <div id="group-<?php echo $group; ?>" class="row-fluid">
            
                
                <!-- Left Bar -->
                <div id="group-<?php echo $group; ?>-left" class="span<?php echo $span_left; ?><?php echo JVERSION < 3 ? ' width-'.$width_left : '' ;?> fltlft">
                    
                    <?php foreach( $fieldsets as  $k => $fieldset ): ?>
                        
                        <?php if( empty($fieldset->align) ) $fieldset->align = 'left' ; ?>
                        <?php if( $fieldset->align == 'right' ) continue; ?>
                        
                        <?php
                        // Tabs & Slides
                        $this->startEditFieldsetPanel($fieldset, $fieldsets) ;
                        ?>
                        
                        
                        
                            <!-- Fieldset -->
                            <?php $this->current_group        = $group ?>
                            <?php $this->current_fieldset     = $fieldset; ?>
                            <?php echo $this->loadTemplate('fieldset'); ?>
                        
                        
                        
                        <?php
                        // Tabs & Slides End
                        $this->endEditFieldsetPanel($fieldset) ;
                        ?>
                        
                    <?php endforeach; ?>
                    
                </div>
                
                
                <!-- Right Bar -->
                <div id="group-<?php echo $group; ?>-right" class="span<?php echo $span_right; ?><?php echo JVERSION < 3 ? ' width-'.$width_right : '' ;?> fltlft">
                    
                    
                    <?php foreach( $fieldsets as  $k => $fieldset ): ?>
                        
                        <?php if( empty($fieldset->align) ) $fieldset->align = 'left' ; ?>
                        <?php if( $fieldset->align == 'left' ) continue; ?>
                        
                        <?php
                        // Tabs & Slides
                        $this->startEditFieldsetPanel($fieldset, $fieldsets) ;
                        ?>
                        
                        
                        
                            <!-- Fieldset -->
                            <?php $this->current_group        = $group ?>
                            <?php $this->current_fieldset     = $fieldset; ?>
                            <?php echo $this->loadTemplate('fieldset'); ?>
                        
                        
                        
                        <?php
                        // Tabs & Slides End
                        $this->endEditFieldsetPanel($fieldset) ;
                        ?>
                        
                    <?php endforeach; ?>
                    
                </div>
            
            </div>
            
            <div class="clr"></div>
            
            <?php echo $tabs ? {{extension.name.cap}}Helper::_('panel.endPanel' , '{{controller.item.name.lower}}Tab' , $group ) : null ; ?>
            
        <?php endforeach; ?>
        
        <?php echo $tabs ? {{extension.name.cap}}Helper::_('panel.endTabs' ) : null ; ?>
    
    
    <!-- Hidden Inputs -->
    <div id="hidden-inputs">
        <input type="hidden" name="option" value="{{extension.element.lower}}" />
        <input type="hidden" name="task" value="" />
        <?php echo JHtml::_('form.token'); ?>
    </div>
    <div class="clr"></div>
</form>

</div>